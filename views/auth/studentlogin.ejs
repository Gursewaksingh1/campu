<%- include('../includes/head.ejs')%>
<link rel="stylesheet" href="/css/forms.css">
<link rel="stylesheet" href="/css/product.css">
</head>

<body>
    <%- include('../includes/navigation.ejs')%>
<main>
    <div>
        <%if (errormsg) {%>
            <%= errormsg%>
            <% } %>
        </div>
        <div>
            <%if (errormsgs) {%>
                <%= errormsgs%>
                <% } %>
            </div>
    <form class="product-form" action="/studentlogin" method="POST">

    <div class="form-control">
        <label for="email">Login</label>
        <input type="email" name="email" id="email" value="<%if (editing) {%> <%= admin.email%> <% } %>">
    </div>
    <div class="form-control">
        <label for="password">password</label>
        <input type="password" name="password" id="password" value="<% if (editing) {%> <%= admin.password%> <% } %>">
    </div>
    <% if (editing) {%>
        <input type="hidden" name="adminId" value="<%= admin._id%>">
        <% } %>
        <button class="btn" type="submit"> Student-Login</button> 
    </form>

</main>
<%-include('../includes/end.ejs')%>