<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/product.css">
</head>

<body>
   <%- include('../includes/studentnav.ejs') %>

    <main>
        <form class="product-form" action="/student/<% if (editing) { %>add-resume<% } else { %>add-resume<% } %>" method="POST" enctype="multipart/form-data">
            <div class="form-control">
                <label for="resume">upload-resume</label>
                <input type="file" name="resume" id="resume" >">
            </div>
            <% if (editing) { %>
                <input type="hidden" value="<%= product._id %>" name="studentid">
            <% } %>

            <button class="btn" type="submit"><% if (editing) { %>Update Product<% } else { %>add-resume <% } %></button>
        </form>

    </main>
<%- include('../includes/end.ejs') %>